# Bugfix Log

Gefundene Bugs und Fixes. Append-only. Format: `- [YYYY-MM-DD] Bug: <...> | Fix: <...> | Datei: <pfad>`.
- [2026-08-05] Bug: openssl `-servername` als type:boolean -> `s_client -servername -connect host:port`, openssl frisst `-connect` als SNI-Wert, Ziel ungenutzt, TLS/SNI-Pruefung kaputt. | Fix: Flag entfernt, `sni:true` am Profil; buildToolCommand leitet SNI immer aus dem Ziel-Host ab (`-servername <host>`); Client-Preview zieht nach. | Datei: API/toolAdapter.js, public/app.js
- [2026-08-05] Bug: serveStatic-Guard per Praefix umgehbar (`startsWith(PUBLIC_DIR)` ohne Pfad-Sep) und Query blieb im Pfad (Assets brachen). | Fix: pathname via `new URL(url).pathname`; Guard `filePath===PUBLIC_DIR || startsWith(PUBLIC_DIR+path.sep)`. | Datei: server.js
- [2026-08-05] Bug: Tool-Sessions ohne Private-IP-Pruefung (startToolSession/startReconBatch delegierten Ziel roh an startTool) -> autorisierter Nutzer konnte Tools gegen 127.0.0.1/interne Hosts fahren (SSRF). | Fix: spawnSession async, `await resolveAllowedTarget(normalized)` blockt Private/Loopback vor dem Spawn; Starter+Routen await. | Datei: server.js
- [2026-08-05] Bug: GET /api/tool-sessions/<id> ohne /events fiel bis serveStatic durch -> 404. | Fix: GET ohne /events liefert `sendJson(200, sessionSummary(session))`. | Datei: server.js
- [2026-08-05] Bug: `crypto.randomUUID()` in server.js (Z.176 assessmentId, Z.272 Session-Id) genutzt, aber `crypto` nicht importiert -> ReferenceError auf Node <=18 in /api/assessments und /api/tool-sessions; Tests fassen die Pfade nicht an, daher unsichtbar. | Fix: `const crypto = require("node:crypto");` am Dateikopf ergaenzt. | Datei: server.js
- [2026-08-05] Bug: Tool-Katalog + Kategorien blieben leer, wenn keine Tools installiert erkannt wurden (renderToolCatalog filterte nach execReady). | Fix: Immer alle Katalog-Tools kategorisiert rendern, nicht installierte markieren (tool-unavailable, Button deaktiviert), Gruppen zeigen verfuegbar/gesamt. | Datei: public/app.js, public/styles.css
- [2026-08-05] Bug: wireStream() schloss EventSource bei jedem "error"-Event sofort (source.close()), was den eingebauten Auto-Reconnect via Last-Event-ID unterband; bei Netz-/Proxy-Unterbrechung fror das Live-Terminal (Einzel-Session + Recon-Batch-Zellen) endgueltig ein, obwohl das Tool serverseitig weiterlief. | Fix: onerror schliesst nicht mehr, sondern zeigt nur einen Hinweis; Reconnect laeuft automatisch, expliziter close() bleibt im exit-Handler. | Datei: public/app.js
